directory for user upload data files

+ ``raw`` the raw folder contains the raw image files that user uploaded, needs user login to access and download the image files in this raw folder.
+ ``large`` the large folder contains the large image file(1080P at least) for view and public access.
+ ``thumbs`` the thumbs folder contains the thumbnail images for the uploads. 